function conviteCasamento() {
    var nomeConvidado = window.prompt("Por favor, insira o nome do convidado:");
    var mensagem = "Você está cordialmente convidado para o nosso casamento, " + nomeConvidado + ". Esperamos vê-lo lá!";
    document.write(mensagem);
}